{
  "name": "fenix-music-backend",
  "version": "2.0.0",
  "scripts": {
    "start": "node server.js",
    "build": "cd ../frontend && npm install && npm run build && mkdir -p ../backend/dist && cp -r build/* ../backend/dist/"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2",
    "pg": "^8.11.3",
    "telegraf": "^4.16.3"
  }
}
